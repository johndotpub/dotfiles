profile: dev
create_home_pyver: false
pyver: "3.12.12"
skel_profile: "default"

# Leave brew_sections unset to install every section from packages/brew.yaml.
# Set apt_sections to opt into specific packages/apt.yaml sections on Linux hosts.
# Example:
# brew_sections:
#   - base
#   - development
# apt_sections:
#   - apt_minimal
