name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set script permissions
        run: chmod +x install.sh bootstrap.sh scripts/*.sh scripts/lib/*.sh test/*.sh

      - name: Install shellcheck (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck bats

      - name: Install shellcheck (macOS)
        if: runner.os == 'macOS'
        run: brew install shellcheck bats-core

      - name: Shell syntax checks
        run: bash -n install.sh bootstrap.sh scripts/*.sh scripts/lib/*.sh test/*.sh

      - name: Shellcheck lint
        run: shellcheck -x install.sh bootstrap.sh scripts/*.sh scripts/lib/*.sh test/*.sh

      - name: Enforce no repo-root config duplicates
        run: ./test/check-no-root-config-duplicates.sh

      - name: Help smoke tests
        run: |
          ./install.sh --help >/dev/null
          ./bootstrap.sh --help >/dev/null

      - name: Dry-run smoke test
        run: ./install.sh --dry-run --no-apt --brew-only --yes --verbose >/dev/null

      - name: Idempotency test
        run: ./test/test-installer-idempotency.sh

      - name: Backup collision test
        run: ./test/test-backup-collision.sh

      - name: Skel merge behavior test
        run: ./test/test-skel-merge-behavior.sh

      - name: BATS installer suite
        run: bats test/installer.bats
